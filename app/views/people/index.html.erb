<p id="notice"><%= notice %></p>

<h1 align="center">Listing People</h1>

<table border="" align="center" id='people_table'>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th colspan="3">Action</th>
    </tr>
  </thead>

  <tbody>
    <% @people.each do |person| %>
       <%= render partial: 'table', locals: {person: person} %>
     <!--  <tr>
        <td><%= person.first_name %></td>
        <td><%= person.last_name %></td>
        <td><%= link_to 'Show', person %></td>
        <td><%= link_to 'Edit', edit_person_path(person) %></td>
        <td><%= link_to 'Delete', person, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr> -->
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Person', new_person_path, 'data-toggle' => 'modal' %>
